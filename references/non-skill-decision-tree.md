# Non-Skill Decision Tree

如果它只是一次性请求，就不要做成 Skill。

如果它会反复出现且规则稳定，先运行 Reuse-First Gate：现有能力完整覆盖时直接复用；核心任务相同但缺少机制时扩展原 Skill；只有职责、输出、权限、工具、生命周期或上下文边界真正独立时才创建新 Skill。

如果它需要大量实时判断但没有固定流程，优先保留成普通对话步骤。

信息盘点不完整时，把唯一性标记为 `missing evidence`，不要直接进入新 Skill 实施。
